import { createAction, createSlice } from "@reduxjs/toolkit";
const initialState = {
    book : [],
}

export const bookSlice = createSlice({
    name: 'book',
    initialState,
    reducers: {
        
    }
})